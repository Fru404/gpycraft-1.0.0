# requirements.txt

# Required dependencies for building documentation with Sphinx
Sphinx==7.2.6
sphinx-rtd-theme==1.0.0  # Replace with the version you prefer

# Optional dependencies (remove if not needed)
better==0.1.5  # Only include if you are using the "better" module
